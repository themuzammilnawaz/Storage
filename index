<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Secure Storage Hub</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<!-- Font Awesome -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

<style>
  body {
    margin: 0;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
    background: radial-gradient(circle at top, #1b2735, #090a0f);
    color: #e5e7eb;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
  }

  .card {
    background: rgba(15, 23, 42, 0.75);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 20px;
    padding: 32px;
    width: 100%;
    max-width: 420px;
    box-shadow: 0 30px 60px rgba(0,0,0,0.6);
  }

  h1 {
    text-align: center;
    font-size: 1.5rem;
    margin: 0 0 16px;
  }

  p {
    text-align: center;
    font-size: 0.9rem;
    color: #cbd5f5;
  }

  input {
    width: 100%;
    padding: 12px;
    margin-top: 12px;
    border-radius: 10px;
    border: none;
    background: #020617;
    color: #e5e7eb;
    font-size: 0.9rem;
  }

  button {
    width: 100%;
    margin-top: 16px;
    padding: 12px;
    border-radius: 12px;
    border: none;
    background: linear-gradient(135deg, #2563eb, #1e40af);
    color: white;
    font-weight: 600;
    cursor: pointer;
  }

  button:hover {
    opacity: 0.9;
  }

  .error {
    margin-top: 10px;
    text-align: center;
    color: #f87171;
    font-size: 0.85rem;
    display: none;
  }

  .links {
    display: none;
    flex-direction: column;
    gap: 16px;
    margin-top: 26px;
  }

  .btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 14px;
    border-radius: 14px;
    font-size: 0.95rem;
    font-weight: 600;
    text-decoration: none;
    color: #fff;
  }

  .drive {
    background: linear-gradient(135deg, #2563eb, #1e40af);
  }

  .mega {
    background: linear-gradient(135deg, #dc2626, #7f1d1d);
  }

  footer {
    margin-top: 24px;
    text-align: center;
    font-size: 0.75rem;
    color: #94a3b8;
  }
</style>
</head>

<body>

<div class="card">

  <!-- LOGIN -->
  <div id="loginBox">
    <h1>Restricted Access</h1>
    <p>Authorized users only</p>

    <input type="text" id="username" placeholder="Username">
    <input type="password" id="password" placeholder="Password">
    <button onclick="checkLogin()">Login</button>

    <div class="error" id="error">Invalid username or password</div>
  </div>

  <!-- STORAGE -->
  <div class="links" id="storageBox">
    <h1>Personal Storage Hub</h1>
    <p>Secure cloud access</p>

    <a class="btn drive" href="PASTE_DRIVE_LINK_HERE" target="_blank">
      <i class="fa-brands fa-google-drive"></i>
      Google Drive
    </a>

    <a class="btn mega" href="PASTE_MEGA_LINK_HERE" target="_blank">
      <i class="fa-solid fa-cloud"></i>
      MEGA Storage
    </a>

    <footer>
      Private · Personal use
    </footer>
  </div>

</div>

<script>
  function checkLogin() {
    const user = document.getElementById("username").value;
    const pass = document.getElementById("password").value;

    if (user === "themuzammilnawaz" && pass === "@Pass(themuzammilnawaz)") {
      document.getElementById("loginBox").style.display = "none";
      document.getElementById("storageBox").style.display = "flex";
    } else {
      document.getElementById("error").style.display = "block";
    }
  }
</script>

</body>
</html>
